import React from 'react';
import './button.less.js';

const Button = () => {
    return (React.createElement("button", { type: "button", className: "btn" }, "read"));
};

export { Button as default };
