import React from 'react';
import './button.less.js';

const Button = () => {
    return (React.createElement("button", { type: "button", className: "btn" }, "123"));
};

export { Button as default };
